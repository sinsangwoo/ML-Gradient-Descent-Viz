# GPU Acceleration Requirements
# =============================
# Install these for GPU-accelerated training

# Base requirements
-r requirements.txt

# JAX with CUDA support
# For CUDA 12:
jax[cuda12]>=0.4.20
# For CUDA 11:
# jax[cuda11_pip]>=0.4.20

# CuPy for GPU arrays
# Choose version matching your CUDA:
# CUDA 12.x:
cupy-cuda12x>=12.0.0
# CUDA 11.x:
# cupy-cuda11x>=12.0.0

# GPU monitoring
nvidia-ml-py3>=7.352.0
pynvml>=11.5.0

# Optional: TensorBoard for visualization
# tensorboard>=2.13.0

# Installation Instructions
# =========================
# 
# 1. Check your CUDA version:
#    nvcc --version
#
# 2. Install matching requirements:
#    For CUDA 12: pip install -r requirements-cuda.txt
#    For CUDA 11: Edit this file and install cuda11 versions
#
# 3. Verify installation:
#    python -c "import jax; print(jax.devices())"
#    python -c "import cupy; print(cupy.cuda.runtime.getDeviceCount())"
#
# GPU Memory Requirements
# =======================
# - MNIST (10k samples): ~2GB GPU memory
# - High-dim (d=1000): ~4GB GPU memory  
# - High-dim (d=10000): ~16GB GPU memory
#
# Recommended GPUs:
# - NVIDIA RTX 3090 (24GB)
# - NVIDIA A100 (40GB/80GB)
# - NVIDIA V100 (16GB/32GB)
